import { CSSObject } from "@mui/material";

const gridContainerCenter: CSSObject = {
  justifyContent: "center",
  alignContent: "center",
  p: 3,
};

const capitalizedText: CSSObject = {
  textTransform: "capitalize",
};

export { gridContainerCenter, capitalizedText };
